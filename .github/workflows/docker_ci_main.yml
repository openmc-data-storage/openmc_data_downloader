
name: CI tests
on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build and test with Docker
      run: |
        mkdir share
        sudo apt-get install -y curl
        docker build -t openmc_data_downloader .
        docker run -v "$PWD/share:/share" openmc_data_downloader
        ll share
        bash <(curl -s https://codecov.io/env) -f share/coverage.xml
        echo trying old curl method
        curl -s https://codecov.io/bash | bash
# -e CI=true
# /bin/bash -c "bash run_tests.sh"


#-e CODECOV_TOKEN=${{ secrets.CODECOV_TOKEN }}